/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "images.dog.ceo",
            "via.placeholder.com"
        ]
    }
}

module.exports = nextConfig
